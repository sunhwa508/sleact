import React from 'react';
import Workspace from '@layouts/Workspace';

const DirectMessage = () => {
  return (
    <Workspace>
      <div>DM!</div>
    </Workspace>
  );
};

export default DirectMessage;
